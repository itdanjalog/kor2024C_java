[배열]
    - 여러 자료/데이터 들을 하나의 자료에 순서대로 저장된 구조
    - 배열 내 모든 요소의 값(자료/데이터)을 순회 할때 주로 반복문 같이 사용된다.
    - 배열 인덱스는 0부터 순서대로 나열된다.
    - 배열과 반복문 관계
        for( int index = 0 ; index < 배열변수.length ; index++ ) {
            배열변수[index]
        }